interface:
  display_name: "Coral Create Source Spec"
  short_description: "Author Coral source specs with lint/add validation"
  default_prompt: "Use $coral-create-source-spec to create or refine a Coral source spec YAML, then validate it with coral source lint and coral source add --file."

policy:
  allow_implicit_invocation: true
